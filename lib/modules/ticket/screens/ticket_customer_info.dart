import 'package:flutter/material.dart';

class TicketCustomerInfoScreen extends StatelessWidget {
  const TicketCustomerInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
